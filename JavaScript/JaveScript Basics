let variable = data; 
const variable = data;

Seven primitive data types
- String : let stringVar = 'Hello';
  - string.reapet(x)
- Number : let num = 8;

console.log(variable); 
- if variable is uninitialized it will print undefined

Non-primitive data type can hold more complex data
- array 
  - let array = []; 
  - index begins at 0
  - can have values of different types 
  - array.length
  - array.push(x) : returns new length of array 
  - array.pop() : returns popped value
  - array.unshift(x) : adds to front of array and returns new length of array
  - array.shift() : remove first element

if () {
} else if () {
} else {
}

for (let i = 0; i < count; i++) {
  console.log(i);
}

function name(parameter) { return x; } 
  function addTwoNumbers(num1, num2) {
    return num1 + num2;
  }
  
  const sum = addTwoNumbers(5, 10);
  console.log(sum)

while (condition) {}

== vs ===
- === strict equality operator checks if two values are equal and share the same type, generally use this
- !== not equal or not same type

// 
/**/

truthy values evaluate to true when evaluated as a boolean 
falsy values are the opposite : false, 0, "", null, undefined, and NaN

